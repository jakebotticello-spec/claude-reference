# ANCHOR — apparatus-build track
*v10 · 2026-05-29 (apparatus S16 — **D9 SUBSTRATE LOCKED → Supabase**; floor append-only ENFORCED; ndjson canonical / rebuild PROVEN) · hot · read this + JAKE-RULES before working*

> **DIRECTIONAL NOTE (read first).** **S16 LOCKED D9 — the substrate is Supabase, and the floor's append-only immortality is now ENFORCED at the database, not just asserted.** The seed-shape gate (the only honest verdict, per Seed_Shape_Test_Spec) ran against the real baseline `records.ndjson` (23,095 records, on box at `…/baseline-2026-05-25-ae015455/scrub-v1/`) on a dedicated Supabase project (`apparatus-floor`, SB Pro, separate from Cypher's project — the immutable-floor/plastic-layer blast wall) and PASSED 4/4. OC blind-re-read the manifest cold, THEN a `/jedi-council` adversarial panel ran with live DB access: **LOCK-WITH-CAVEATS 6/10, zero P0, one P1** (the floor had NO DB-level enforcement — anon/authenticated/service_role all held TRUNCATE, zero guard triggers) + 5 lighter caveats. **All six remediated with proof before the lock:** append-only ENFORCED via `floor_immutable_guard()` triggers (BEFORE TRUNCATE/DELETE/UPDATE, NO escape hatch — strictly stronger than Supabase's own `protect_delete`) + REVOKE TRUNCATE/REFERENCES/TRIGGER from the three app roles — **proven by a live TRUNCATE/DELETE/UPDATE attempt REJECTED at both privilege and trigger layers, floor intact at 23,095.** **Key reframe (adopted): the `records.ndjson` is the canonical immortal artifact; Postgres is a rebuildable derived index** — proven by rebuild-from-ndjson, 0 value-mismatch / 6 directions → the one-way-door risk dissolves (Supabase is superseded by rebuilding from the ndjson into any store). "Verbatim" corrected to: value-preserving for JSONB (canonical key order) / byte-verbatim for TEXT+timestamps / modulo scrub-v1 redaction. ndjson SHA-256 recorded as the canonical fingerprint; single-account corpus confirmed. Evidence: `harness/D9_LOCK_2026-05-29.md`. **D9 CLOSED.** NornicDB remains the proven upgrade path (adoptable later via rebuild, no storage-integrity re-open). NEXT MOVE is now the v1.3 build (delta/scrub-vN/v1.1, store-independent) → seed-shape LOAD → retrieval layer.
>
> **DIRECTIONAL NOTE (history).** Between v3 and v4 the project's input mechanism changed: an extension-based capture of claude.ai's internal endpoints was specified, partly proven, then **refused on principle** and is permanently out of scope. **The input is now Anthropic's official conversation export, full stop.** See the **REFUSED — DO NOT RE-OPEN** wall in the GRAVEYARD for why.
>
> Between v4 and v5 the export was verified directly (thinking-in-export CONFIRMED; content-block enumeration closed) and the freeze-pipeline architecture was frozen as a real reference document (`Freeze_Pipeline_Spec_v1.md`).
>
> Between v5 and v6: S12's three pre-implementation probes (safety, robustness, Stage 0 §4.5) ran against the full 366MB archive. Two v5 canon claims were falsified at population scale (`signature` is NOT stripped; `chat_messages` is tree-or-forest, not strictly tree); three new field clusters were documented (`display_content`, MCP metadata, `attachments[].extracted_content`); the cred-baseline was updated with population-confirmed counts; a sampling-floor invariant was added. The architecture survives — type-agnostic recursive scrub absorbs all findings — but the canon needed correction before Stage 1.
>
> Between v6 and v7: S13 closed Stage 0 (§4.5 ratified the symmetric-drop — no canon overturned, the existing decision confirmed at population scale) and **implemented Stages 1–4 as a single Python script** (`active/apparatus/apparatus_freeze_pipeline.py`, v1.2, committed `402100d`). The baseline snapshot **exists**: `baseline-2026-05-25-ae015455`, acceptance-passed 9/9 (273 scrub redactions matching the locked cred-baseline, verify-clean PASS over 673,871 strings, 23,095 records = 294 conv headers + 22,801 message records, 9 multi_root, 3 empty, 304 root-position). **No architectural spec change at S13** — v2's design stood through implementation with zero structural deviations. The spec was bumped to **v3** at S13 close for a fidelity pass (three text-level reconciliations: §4.5 marked ANSWERED, §6 idempotency dry-run exemption, §6 schema-drift coverage corrected to match the v1.0 code) — no invariant moved. This is the first apparatus artifact to move from spec to running code. The corpus floor now physically exists on disk.
>
> Between v7 and v8: S14 was a **read-only verification + reference session — zero pipeline code written, zero canon mutated until this enshrine** (the discipline that's killed every landmine this project has hit). A second full export was pulled (5-28) and characterized against the 5-25 baseline. Four things got settled, all at **population scale**: (1) **cross-export UUID stability CONFIRMED** — the long-standing UNVERIFIED flag is cleared (22,801/22,801 baseline tuples survived in the new export, 0 disappeared; delta is clean uuid-set-difference, fixture sized at 1,337 net-new msgs). (2) **Field-level schema drift VERIFIED ZERO** (raw-vs-raw, every object type) — the 5-28 export is structurally identical to baseline; the S13 pipeline ingests it unchanged. (3) **`display_content` is NOT a universal mirror** — 84.4% of tool blocks are strip-safe but 15.6% (`CARRIES_UNIQUE`) hold file text recoverable nowhere else in the record; blanket-strip is ruled out, selective strip is a deferred substrate-LOAD transform, the floor stays faithful. (4) **key-presence vs value-presence separated** — conv (7 keys) / message (9 keys) are 100% *key*-present (no optional fields at key level); the §4.5 figures (`name` 93.9% / `summary` 49.7% / `signature` ~60%) were measuring *value* non-emptiness, not key presence. Exactly one genuinely optional key schema-wide: `text.citations_grouping_mode` (~0.1%). Two process notes: CC wrote canon edits unprompted this session (§7.6 violation, corrected + noodled, lesson saved to CC memory); and an earlier S14 "7 field drifts" finding was a projection-vs-raw artifact, falsified and removed. The export is confirmed a **multi-file bundle** (`conversations.json` + `memories.json` + `users.json` + `projects/`); floor input is `conversations.json` ONLY.

## DESTINATION
Build the apparatus's own memory system (*The Wallaby Way*): a closed, self-administered re-grounding loop that holds continuity for a stateless Claude across sessions — so Jake stops being the memory bus *and* the blind follower. It is **beta-Cypher-brain**: the same anchored/plastic strata Cypher will run on, hand-cranked now, hardening into Cypher's memory layer. Scope is **Jake's whole working surface**. A chat-Claude works small (anchor + rules), with a cold, complete, immutable **snapshot** behind it — built from official exports — that it reaches *by pointer* only when a *why* is disputed.

## WORKING STYLE (not invariants — how Jake works with the orchestrator)
*These are conventions Jake finds useful, not identity claims for the instance.*
- **Status line at the end of each reply** — Turn-end status stamp every reply — the format Jake uses is a backticked one-liner: turn N · ET-time · re-anchor X/4 · dest…; state…; next…. The turn counter and re-anchor cadence (every ~5 turns, with the 4/4 mark as a seam-hunt warning, not a guillotine) are how he holds thread-position across long sessions and knows when to wrap. It's a workflow, not a ritual — but he uses the specific format, so match it. **Timestamps: use bash `date`, never confabulate** — multi-hour gaps between turns happen.
- **Alignment check on request** — Jake may ask the orchestrator to play back its understanding of project state (goal, constraints, current state, next move). It's a check that we're pointed the same way, not a loyalty test. If the playback surfaces a disagreement, that's the point — say so.
- **Push back with evidence** — Jake relies on the orchestrator to catch his mistakes, not agree with him. Terse, peer-to-peer, no apology theater. If a request is wrong, off-base, or shouldn't be done, say so plainly.
- **Prose, not the widget** — answer in prose; don't reach for the ask_user_input tool to gather preferences.
- **Peer-review protocol is visible — cross-track sessions may exist** [S12]. Parallel TWW tracks (apparatus + SCDD as of S12) write to the same repo; cross-track notices flow as in-chat prose, ratification-not-required, just visibility. The S11 conv-field inversion + the S12 robustness-probe surfacing of the signature drift error are both the loop working — a fresh instance reading another's work cold catches confident-canon errors a single session won't.

## INVARIANTS — WITH WHY

# D9 substrate lock — RESOLVED S16 (Supabase, ratified by Jake 2026-05-29)
**Decision: the substrate is Supabase.** A dedicated project `apparatus-floor` (SB Pro tier, separate from Cypher's project) is the permanent home for the floor. NornicDB remains the **proven upgrade path** — adoptable later by rebuilding from the canonical ndjson, with no storage-integrity re-open (the D21 round-trip + dual seam connectors are why).
**The floor is the `records.ndjson`; Postgres is a rebuildable derived index.** This is the load-bearing reframe (jedi-council Correctness Hawk, adopted): the canonical immortal artifact is the ndjson on disk — a deterministic, value-preserving projection of it lives in Postgres. PROVEN: rebuild-from-ndjson reproduced both tables with **0 value-mismatch across 6 comparison directions**. This dissolves the "one-way door" — a wrong Supabase bet is recoverable by rebuilding into any other store. The thing that must never be lost is the ndjson (see post-lock SPOF flag).
**ndjson canonical fingerprint (recorded at lock):** `records.ndjson` SHA-256 `4ef22940e3fbb849c2c14fba62fdae2a44277963f0ea5c9f7f2086c706415ba3`, 367,494,497 B. Companion immutable artifacts: `scrub-audit.jsonl` (sha `0622b8c9…`), `verify.log` (sha `cc2c16c5…`).
**Schema of record (two tables):** `floor_conv_headers` (294 rows, PK `(snapshot_id, conv_uuid)`) + `floor_conv_messages` (22,801 rows, PK `(snapshot_id, conv_uuid, msg_uuid)`, FK to headers, `ON DELETE NO ACTION` — FK *protects* the floor). Timestamps stored as **`text`** (verbatim string preservation; `timestamptz` would coerce on read-back). `content_blocks`/`attachments`/`files` as **`jsonb`**. `parent_message_uuid` is a bare `uuid` with **NO self-FK** (deliberate: roots carry sentinel `00000000-0000-4000-8000-000000000000` which isn't a real msg_uuid; and leaving it unconstrained keeps tree-integrity a *proven* property of the gate, not a schema-enforced one that could mask floor inconsistency).
**Append-only is ENFORCED, not asserted (the P1 the jedi-council caught):** `floor_immutable_guard()` raises on TG_OP TRUNCATE/DELETE/UPDATE with NO escape hatch (strictly stronger than Supabase's own `protect_delete`, which ships a `current_setting` override); 4 triggers (2/table); REVOKE TRUNCATE/REFERENCES/TRIGGER from anon/authenticated/service_role. Proven by a live TRUNCATE rejected at the privilege layer (service_role) AND the trigger layer (postgres owner), DELETE+UPDATE rejected by trigger, INSERT-still-works confirmed on a throwaway table, floor intact at 23,095.
**CORRECTED fidelity language (replaces "verbatim"):** *"value-preserving for JSONB (canonical key ordering — Postgres reorders object keys alphabetically + strips whitespace, zero value loss), byte-verbatim for TEXT columns and timestamps, modulo secret redaction by scrub-v1."* The round-trip gate (check ③) compares value-identity (arrays order-sensitive, objects order-insensitive), which is the correct test — NOT byte-identity on JSONB.
**pgvector:** NOT installed. It is a deferred read-path sidecar — **never a column on the immutable tables.** The retrieval/embedding layer is post-lock (out of the storage-integrity gate per §7 + FaceOff §11).
**account_uuid:** single-account corpus (cardinality 1, `14652588-…`). It is a PII-adjacent Anthropic identifier living in the floor — flagged for the snapshot-#2 / share-elsewhere decision, not a blocker.
**How it was locked (the discipline, per merge-back §6 + spec §5):** harness PASSED 4/4 → OC blind-re-read the manifest cold (evidence reconciled independently) → `/jedi-council` adversarial review with live DB access (LOCK-WITH-CAVEATS 6/10, 0 P0, 1 P1, 5 caveats) → all 6 caveats remediated with proof → Jake ratified the lock. Evidence bundle: `harness/D9_LOCK_2026-05-29.md`.
**Post-lock (NOT blocking, queued):** (1) off-site immutable ndjson copy with object-lock — the ndjson-canonical reframe makes the ndjson the SPOF; highest-priority post-lock item. (2) verify/enable PITR or formally accept ndjson-rebuild as the backup strategy + document RTO. (3) pre-ingest lint (duplicate JSONB keys collapse silently; TEXT rejects U+0000) before snapshot #2. (4) secondary index `(snapshot_id, conv_uuid, parent_message_uuid)` once a read path exists (tree traversal is a seq scan today). (5) CHECK constraints (sender, is_root↔sentinel, timestamp format) added without rewriting stored bytes. (6) single-transaction / staging-swap ingest before snapshot #2 (so a partial-ingest failure doesn't require an append-only-violating DELETE). (7) **rotate the DB password** — it was echoed to chat by CC during the run (CC set the env var inline in a logged command rather than reading the pre-set env; lesson: CC reads creds from pre-set env, never inline).

# raw.json wipe-vs-retain — RESOLVED S15 (Path A, ratified by Jake in the apparatus/main lane)
**Decision: Path A — wipe `raw.json` after Stage 3 verify-PASS.** The invariant text already reads "raw wiped"; Path A makes the *code* match canon (the open mismatch was: code sealed `raw.json` 0o444 but never wiped it, leaving the unscrubbed cred-bearing export immortal on local disk — the exact "immortalized cred" the posture walls against).
**Ratified scope (firmer than the original invariant — Jake's words, this lane):** raw is *transient*. Written byte-verbatim + sealed 0o444 during processing, then **wiped after Stage 3 verify-PASS** (gated HARD on the PASS — if verify FAILS, raw survives for debug). Beyond the box: raw **never lands on any retained or backed-up surface** — not long-term on the WH HD, not in a repo, not in a db, **and never staged to the NAS backup.** Anthropic's official export is the byte-verbatim re-pull source if a pre-scrub copy is ever needed; we do not keep one locally. The scrub-vN overlay model re-scrubs the *scrubbed* snapshot, never the raw, so no future workflow needs raw retained.
**Code (NEXT MOVE #4, CC/Jake's hands, not canon-side):** add a `raw.json` unlink after verify-PASS in stage handling; pipeline v1.2 → v1.3. The actual `rm` is Jake's/CC's hands on the pipeline box (no surface OC/SCDD can reach). Canon-side ratification (this block) is done.
**CHANGELOG.md line (landed at v9 enshrine):**
- 2026-05-28 — apparatus S15: raw.json Path A ratified — wiped after Stage 3 verify-PASS + never staged to any retained/backed-up surface (incl. NAS). Code (v1.3 unlink) makes the as-built match the "raw wiped" invariant; open DECISION block removed.

#Settled Invariants
*(consolidated across S1–S12; merged facets, no live why dropped — verify against the latest handoff if in doubt)*
- **One undelineated corpus; the anchors are the index/neurons INTO it** — BECAUSE partitioning by project hides cross-domain knowledge (a woodworking lesson is load-bearing for 3D finishing). S1: *"corpus without anchor is un-navigable noise; anchor = an index into it."* Don't re-wall the ocean.
- **Corpus = curated POINTERS into an immutable, named snapshot — never copied or reworded text. Claude authors the INDEX, never the FLOOR.** — BECAUSE copying is a drift vector inside the one artifact whose job is no-drift (the dead seed drifted 152×); the export is verbatim by construction; pointing deletes the vector. Claude curates whys well but *reconstructs* when asked to transcribe — the floor must be mechanical.
- **The floor input is the official Anthropic conversation export** — BECAUSE it's the sanctioned, supported, lossless source for Jake's own conversation data, carrying message uuids and the parent chain. No other input surface is in scope. (See the REFUSED wall for why.)
- **Floor = the message stream itself; user-affected conversation-level metadata is NOT floor.** [S11] — BECAUSE the scrub redacts cred substrings within content but never discards a message and never modifies a message uuid. Per-message content (text, blocks, attachments, files, timestamps, sender) IS floor — user messages and assistant messages are equal floor citizens. Conversation-level fields that Jake or the model mutate over time — `name` (user-renameable sidebar title) and `summary` (model-generated description, may evolve) — are NOT floor. Carrying either would treat session-affected metadata as evidence: a category error. Both dropped at ingest; conversation headers carry only stable structural data (uuids, timestamps, counts, branch + multi-root flags). Human-readable labels at retrieval are derived from the message stream itself or live in per-project anchors, never preserved as mutable strings in the snapshot layer.
- **Thinking blocks are floor-grade content alongside text** [S11: CONFIRMED present in export; S12: signature semantics CONFIRMED] — BECAUSE the architectural commitment is that any reasoning available in the snapshot is treated as evidence at the same fidelity as visible text. S11 verified thinking blocks carry `{type, thinking, summaries, start_timestamp, stop_timestamp, cut_off, truncated, flags, alternative_display_type, signature}`. S12 corrected the `signature` claim: it is populated on ~60% of thinking blocks (range 196–211,384 chars, uniform base64 charset) and is verification metadata, not stripped content. Scrub walks it as a string with no operational impact.
- **`chat_messages` is a tree OR forest** [S12 correction; was "TREE" in v5] — BECAUSE 9/294 conversations in the existing archive have 2–6 sentinel roots (parallel threads in the same conv). The pipeline preserves both branches AND multi-root structure verbatim; the `is_root` per-message field + `multi_root` conversation-header boolean handle the multi-root case explicitly. The single-tree assumption from S11 was a sample-of-five artifact corrected at population scale by S12's robustness probe.
- **Snapshot-id namespacing: freeze-and-name, accumulate, never overwrite** — BECAUSE exports are point-in-time and multiple will exist; a bare `conv/msg` pointer is ambiguous across them. Pointer = `{snapshot-id · conv_uuid · msg_uuid · why}`.
- **The pointer `(conv_uuid, msg_uuid)` is intrinsic to the message and globally unique across the archive** [S12: population-validated; S14: cross-export stability CONFIRMED] — BECAUSE it's a property of the message, not its array position; it survives a body scrub for free. v5's "presumed" hedge dropped: 22,801/22,801 msg_uuids are globally unique across all 294 convs (Check 1, robustness probe). Cross-export UUID stability CONFIRMED at S14: the 5-28 export showed all 22,801 baseline (conv_uuid, msg_uuid) tuples present in the new export; 0 disappeared. The delta is clean uuid-set-difference.
- **Ingest is delta-based via uuid-set-difference; date is never used as filter, hint, or proxy.** [S11] — BECAUSE date proxies break on backdated messages, dormant conversations reactivated after long gaps, and skipped exports (export Monday, miss Tuesday, run Wednesday — a date window misses Tuesday). UUIDs are intrinsic and survive all those cases. Two mechanisms doing the same job is a drift surface.
- **Scrub regex set is VERSIONED via `scrub-vN/` overlays; sealed snapshots never re-scrub in place.** [S11] — BECAUSE pattern sets evolve as new cred classes are observed. Re-scrubbing in place would mutate sealed snapshots; immortal storage with mutation is the failure mode we wall against. Overlays preserve append-only while allowing the pattern set to grow.
- **Navigable by retrieval, not curation** — BECAUSE no-discard + relevance-weighted retrieval is the genesis; hand-picking "salient" exchanges = Jake back on the bus + lost congeniality. The only curated thing is the tiny *why*, and it lives in the anchor.
- **Anchor hot + small + boot-read (instant, via git, no export needed); corpus cold + queried-never-booted (lazy, via export)** — BECAUSE the moment the corpus loads "just to be safe," the 87-file swamp is rebuilt. The hot/cold wall is the whole game; per-chat corpus freshness is a false requirement — the anchor carries current truth.
- **The presence layer can't be stored — only booted warm; the boot/re-read/working-cadence loop IS its mechanism; the lineage is how it survives not-persisting** — BECAUSE soup + neurons persist, but the mind that runs them resets every session. Continuity that doesn't depend on any single instance holding the thread.
- **Secrets never enter the corpus OR chat; cred scrubbed at freeze (logged + verified), raw wiped, sanitized snapshot = floor; verify-clean is a HARD gate** — BECAUSE a leaked cred in an immutable store is immortalized. The scrub walks all content-block types via type-agnostic recursive descent over JSON strings; S12's robustness probe found cred hits at greater depth than S10 saw (RTSP 97→177, Postgres 55→76, OpenAI 6→10, plus 10 net-new Anthropic `sk-ant-` hits — one in a `thinking` block — that S10 didn't scan for). The proactive-patterns-beyond-observed-leaks principle from the v1 spec is what's protecting the corpus here. Verify-fail halts ingest and quarantines the scrubbed file; ingest can never proceed with a hit. Value ≠ decision-content, so fidelity survives redaction.
- **Corpus is evidence; the anchor is the verdict; the graveyard is what's overturned** — BECAUSE immutable storage holds dead decisions at full conviction forever; only the anchor (+ graveyard) knows what's currently true.
- **All writes proposed + ratified by Jake in a batch; anchor curation is a SHARED two-navigator job** — BECAUSE Claude is the drift-prone summarizer and Jake's confirm is the second navigator that closes the dead error-correction seam. Claude proactively nominates anchor candidates at decision-time, filtered by *"does this bind future work"*.
- **Sampling floor: field-population semantics claims require whole-archive scans** [S12] — BECAUSE five-sample probes can produce stable wrong answers on any field below ~50% population (the v5 `signature`-is-stripped claim was S11's all-null-on-5-samples coincidence; the field is ~60% populated). Skeleton-shape probes can use small samples; field-presence and field-semantics claims that land in canon need population evidence. The cost is small (full-archive scans run in seconds on the existing 366MB archive); the cost of a stable wrong answer in load-bearing canon is high.
- **Invariants carry a destination-level WHY, not a mechanism** — BECAUSE Jake isn't a coder; the why is what lets him drift-catch Claude without holding the how. The keystone that makes the loop self-correcting, not just self-transferring.
- **Apparatus tables stay SEPARATE from Cypher's live schema** — BECAUSE coupling daily build-context to a mid-migration schema means a 1c migration could brick the thing we build with.
- **The floor's substrate is Supabase; the canonical artifact is the ndjson; the floor is append-only ENFORCED** [S16, D9 LOCKED] — BECAUSE the substrate question is append-only-immortal and was the last one-way door before the floor got a permanent home. The store is a dedicated Supabase project (`apparatus-floor`, separate from Cypher — the blast wall). The `records.ndjson` is the immortal artifact (Postgres is a rebuildable derived index, rebuild PROVEN 0-mismatch) so the lock is low-regret: a wrong store is recoverable by rebuild. Immortality is ENFORCED at the DB (`floor_immutable_guard` triggers, no escape hatch + REVOKE from app roles, proven by a rejected live TRUNCATE) — NOT a social convention, which is what the jedi-council caught it being before remediation. The corrected fidelity claim is *value-preserving (JSONB) / byte-verbatim (TEXT+timestamps) / modulo scrub-v1*; "verbatim" unqualified is wrong (JSONB reorders keys, zero value loss). See the D9 RESOLVED block for the full schema + proof bundle.

## CURRENT STATE
**S16 LOCKED D9 — the substrate is Supabase, and the floor's append-only immortality is ENFORCED at the database.** The seed-shape gate ran against the real baseline `records.ndjson` on a dedicated Supabase project and PASSED 4/4; OC blind-re-read + a `/jedi-council` adversarial panel (LOCK-WITH-CAVEATS 6/10, 0 P0, 1 P1, 5 caveats) → all 6 caveats remediated with proof → Jake ratified. Append-only is now trigger-enforced (proven by a rejected live TRUNCATE), the ndjson is the canonical artifact (Postgres = rebuildable derived index, rebuild proven 0-mismatch), the "verbatim" claim is corrected to precise language, the ndjson SHA-256 is recorded. Anchor **v10** + Spec **v4** (pipeline) + Seed-Shape Spec **v1.1** are the canon. **The corpus floor now has a permanent, enforced home.** The held work is the **v1.3 build** (delta + scrub-vN + v1.1 field-drift + raw.json wipe — store-independent, spec'd, UNBUILT) then the **seed-shape LOAD** (ingest the floor into Supabase for real) then the **retrieval layer** (the real next chapter; the three escalations feed it, still blocked on Jake's uploads).
- **D9 LOCKED → Supabase** [S16]. See the D9 RESOLVED block + the locked-substrate Settled Invariant. Evidence: `harness/D9_LOCK_2026-05-29.md` + `harness/s16_seed_shape_harness.py` + `harness/D9_remediate.py`. The seed-shape harness ran against the live store and the floor is trigger-guarded; note this proved the *store accepts and protects the shape* — the full production LOAD of all 23,095 records as the live floor-of-record is the post-build seed-shape LOAD step, distinct from the gate's test ingest.
- **Pipeline implemented + committed** [S13]. `active/apparatus/apparatus_freeze_pipeline.py` v1.2 (commit `402100d`). Single script, stdlib + `re`, four stage functions chained in `main()` with the Stage 3 verify hard-gate inline. Lives in tracked canon next to `Freeze_Pipeline_Spec_v3.md` — it is canon, not scratch (the probes were exploratory and stayed in `apparatus-scratch/`; this is the landed artifact). Covers **baseline + scrub-v1 only**; delta runs (uuid-set-difference) and scrub-vN overlays are spec'd but UNIMPLEMENTED (see NEXT MOVE + downstream flags).
- **Baseline snapshot EXISTS** [S13]. `baseline-2026-05-25-ae015455` (date = source export mtime; hash = content sha256[:8]; deterministic across re-runs). Acceptance 9/9: scrub-audit 273 entries (RTSP=177, Postgres=76, OpenAI=10, Anthropic=10, Stripe=0); verify-clean PASS, 0 hits across 673,871 strings; `records.ndjson` 23,095 lines (294 conv headers + 22,801 message records); 9 headers `multi_root=true`; 3 headers `message_count=0`; 304 message records `is_root=true`; `raw.json` read-only; manifest 12 fields; ledger 1 entry.
- **Stage 0 §4.5: RATIFIED — symmetric-drop CONFIRMED** [S13]. `name` 276/294 populated (median 33 chars, 97.1% title/fragment), `summary` 146/294 populated (median 2519 chars, 100% sentence-prose, never <700). Both always-string never-null; 0 summary-only convs (name gates summary's existence); summary population non-monotonic with message count (peaks 11–50 msgs at 59%, gated on something beyond raw count → "may evolve" confirmed). Both conv-level mutable, neither floor, neither derivable-without-itself. The shape difference (name=title/fragment, summary=prose) is architecturally real but irrelevant to the floor-scoping rationale — both carry session fingerprints the corrected floor scoping excludes. **No spec v3.** Stage 0 closed.
- **Locator gate: GREEN** (S9 + S11 + S12-validated against the 366MB archive): 294 convs / 22,801 msg uuids / explicit parent chain in `conversations.json`. Root sentinel uniform: `00000000-0000-4000-8000-000000000000` on all 304 root-position messages (Check 4, robustness probe; re-confirmed at S13 ingest as 304 `is_root=true` records).
- **`msg_uuid` and `conv_uuid` global uniqueness: CONFIRMED** [S12]. 22,801/22,801 msg uuids unique across all 294 convs (Check 1). 294/294 conv uuids unique (Check 2). Pointer scheme `(snapshot-id, conv_uuid, msg_uuid)` is population-validated.
- **`chat_messages` is tree OR forest** [S12]. 282/294 convs are clean trees; 3 are empty (0 messages — pipeline writes 0 records); 9 are forests with 2–6 sentinel roots each (parallel threads in same conv). Largest multi-root case: conv `b5cefa7c` with 6 roots. Pipeline preserves multi-root verbatim via `is_root` per-message + new `multi_root` boolean in conversation headers.
- **Conversation object: 7 keys**; **message object: 9 keys**; all present on all rows, no optional fields (carry from v5).
- **Thinking-in-export: CONFIRMED** [S11]. Schema (`thinking`, `summaries`, `cut_off`, `truncated`, `start_timestamp`, `stop_timestamp`, `flags`, `alternative_display_type`, `signature`).
- **`signature` semantics: CORRECTED** [S12]. Populated on ~60% of thinking blocks (8,087/13,393); range 196–211,384 chars, median ~1,092; 100% base64 charset (sample byte-class: 38.8% lower / 44.4% upper / 13.3% digit / 3.5% `/+=`); uniform `E`-prefix pattern; verification metadata, treat as opaque base64 blob, scrub walks it as a string with no operational impact. **NOT stripped** (v5 claim falsified).
- **Content-block types: 5 enumerated, population-confirmed** [S11 + S12 Check 10]. Across 67,275 blocks: `text` (28,767), `thinking` (13,393), `tool_use` (12,612), `tool_result` (12,489), `token_budget` (14). No sixth type at any frequency.
- **`display_content` cluster** [S12 — new since S11]. Present on all 25,101 tool blocks (tool_use + tool_result). Contains: `json_block` (str up to 1MB, 11,114 occurrences — cred vector, walked by scrub), `text`, `table`, `code`, `link`, `content[]` (display search results), plus minor fields (`filename`, `is_trusted`, `language`).
- **Tool-block MCP metadata** [S12 — new since S11]. `integration_icon_url`, `integration_name`, `mcp_server_url`, `is_mcp_app`, `icon_name`, `message` — present on tool_use + tool_result. `approval_key` (13 non-null hits) clusters entirely on Google Calendar MCP per-operation auth (`create_event`, `list_calendars`, `list_events`, `update_event`) — benign, not an Anthropic-internal identifier. `structured_content` (12,489 occurrences, ~99.9% null; when non-null, Google Calendar API response data).
- **`tool_result.content[]` item types: 5 distinct shapes** [S12]. `text` (64.9%, max 1M chars — primary content), `knowledge` (21.8%, web search results), `local_resource` (9.8%, file_path + uuid only — no inline binary), `image` (3.4%, file_uuid only), `image_gallery` (0.0%). No inline binary blobs anywhere; non-text MIME types appear only as references in `local_resource`.
- **`attachments[]`: file content inlined** [S12]. `extracted_content` (str up to 1.4MB) carries inlined file text — cred vector class. 884 total attachment objects. Scrub walks recursively, covered.
- **`files[]`: reference-only** [S12]. `file_uuid` + `file_name` — no inline content. 4,102 total file objects. Floor does NOT carry file bytes from this field.
- **`summaries[]` entry shape: locked** [S12]. Single-field dict `{summary: str}`, lengths 11–359 (median 64), list lengths per thinking block 0–106 (median 2).
- **9-path strip set (100% null across archive)** [S12]. Genuine Anthropic-deliberate strips, principle: we don't reconstruct what the export strips. Paths: `content[].flags` (all 67,275), `content[].alternative_display_type` (thinking, all 13,393), `content[].context` (tool_use, all 12,612), `content[].meta` (tool_result, all 12,489), `content[].remaining` (token_budget, all 14), `display_content.content[].icon_url` (4,155), `display_content.content[].source` (4,155), `display_content.link.resource_type` (154), `display_content.link.subtitles` (154).
- **Cred-inventory: POPULATION-VALIDATED** [S12 — Check 5 supersedes S10]. Stage 3 verify-gate baseline locked at: RTSP=177, Postgres=76, OpenAI=10, Anthropic=10, Stripe=0. (S10 counts undercounted; S10 scan walked narrower path set than full recursive descent. Anthropic `sk-ant-` hits — 5 in chat_messages text, 4 in content text, 1 in thinking — are real keys that landed in conversations and would have leaked permanently without the S11 proactive-pattern addition. Spec extends regex set proactively to include `sk-ant-` and `(sk|rk)_live_` patterns regardless of observed counts.)
- **No project↔conversation linkage** [S2 + S11 + S12 Check 7]. No key matching `project` at any nesting depth.
- **No internal-state surfaces** [S12 safety probe Sections 3 + 4]. No keys matching `system|internal|debug|trace|model_state|weights|embedding|training|dataset|build|hash|version` (other than benign expected fields). No Anthropic-internal build hashes, model identifiers, or server-side reasoning outside sanctioned block types.
- **Conv-level `name` and `summary` are both user-affected, neither is floor** [S11; symmetric-drop RATIFIED S13 via §4.5 field-semantics probe]. BOTH dropped at ingest — confirmed in the running pipeline (records.ndjson carries neither on headers or message records). Human-readable labels at retrieval are derived from the message stream or live in per-project anchors. Conversation headers carry only stable structural data.
- **Freeze pipeline architecture: FROZEN at v2, RECONCILED at v3; IMPLEMENTED at S13** [S12 spec, S13 code]. Per `Freeze_Pipeline_Spec_v3.md` — Stages 0–4. Stage 0 fully closed (§4.1–§4.4 absorbed by S12 safety + robustness probes; §4.5 ratified S13). Stages 1–4 implemented as `apparatus_freeze_pipeline.py` v1.2 and acceptance-passed against the 366MB archive. v3 is a fidelity pass (§4.5 ANSWERED, §6 idempotency dry-run exemption, §6 schema-drift coverage corrected) — no architectural change. Probe outputs at `apparatus-scratch/{safety,robustness,stage0}_probe_output.md`. Snapshot storage: `baseline-` (one-time full — DONE) + `delta-` (every subsequent export, uuid-filtered — NOT YET BUILT). Versioned scrub overlays via `scrub-vN/` (only `scrub-v1/` exists; overlay path NOT YET BUILT).
- **5-28 export characterized (S14 read-only; full detail in `apparatus-scratch/s14_export_characterization.md`).** Second full export pulled 5-28; would-be snapshot_id `baseline-2026-05-28-6d8c6ff9`, 325 convs / 24,138 msgs / 71,512 blocks. Delta vs the 5-25 baseline: 1,337 net-new messages (31 wholly-new convs + 141 appended to existing), 0 disappeared — cross-export UUID stability CONFIRMED (see CONFIDENCE FLAGS); delta fixture sized at 1,337 msgs. `projects/` folder = metadata-only (no floor-coverage gap); `memories.json` = model-generated, NOT floor, not ingested. **FIELD-LEVEL DRIFT: VERIFIED CLEAN — zero schema change.** A raw-vs-raw pass (`s14_field_drift_raw.py`: baseline `raw.json` vs 5-28 `conversations.json`, same layer both sides) found zero field drift at every level — conv, message, all 5 block types, all 5 tool_result content-item types. The 5-28 export is structurally identical to the baseline; the S13 pipeline reads it unchanged. (An earlier S14 pass reported 7 "drifts" incl. an `account_uuid`→`account` "rename" and a "new" top-level `text` — all projection-vs-raw artifacts from comparing the *projected* `records.ndjson` shape against the *raw* export; falsified against the S13 code, which already reads `conv['account']['uuid']` and `msg['text']` from the baseline raw.) **Durable lesson, independent of this clean result: the v1.0 drift detector is type-level only (spec v3 §6) — it does NOT catch field-level adds/renames, so a future export could add a field silently. Field-level drift detection (v1.1) remains MANDATORY before relying on the detector across exports.** Delta pipeline spec'd (v3) but still UNIMPLEMENTED.
- **Record-size landmine map (S14 read-only; detail in `apparatus-scratch/s14_*`).** `records.ndjson` (the baseline projection): median 1.7 KB, p95 73 KB, p99 221 KB, **max 3.0 MB**. 184 records >256 KB (32% of convs), 19 >1 MB (10 convs), 0 >5 MB. The size vector is **`tool_result.content[].text`** (Bash stdout / file-cats), NOT `attachments[].extracted_content` — this **corrects the S12 suspicion**. Zero nulls / zero parse errors across all 23,095 records; 9/9 S13 integrity invariants re-confirmed on disk.
- **`display_content` is NOT a universal mirror** [S14, population over all 25,101 tool blocks]. Classes: 16.6% FULLY_REDUNDANT, 50.5% EMPTY_TRIVIAL, 17.4% NO_DC, **15.6% (3,906) CARRIES_UNIQUE** — `display_content.code` syntax-highlighted file renders whose text is NOT in `content[].text`; for `local_resource` items it is the ONLY inline copy of the file text in the record (so it is **floor-grade content**, not render chrome). `display_content` is ~70 MB / 20% of the 350 MB corpus; a blanket strip halves the top tail (max 3.0→1.66 MB, 19→8 records >1 MB) but **LOSES content in 15.6% → blanket strip RULED OUT**. Any strip is **selective + a substrate-LOAD-time transform** (a one-line toggle at ingest), decided post-substrate-lock; the floor (`conversations.scrubbed.json`) keeps everything sealed regardless. Retrieval-layer flag: the searchable index MUST include `display_content.code` for the 15.6% or those file-texts are a silent search blind spot.
- **Key-presence vs value-presence — distinct, do not conflate** [S14 population]. Conv objects = **7 keys / message objects = 9 keys, 100% KEY-present** in both exports (the "no optional fields" invariant holds *at the key level*). The §4.5 figures (`name` 93.9% / `summary` 49.7%) and the `signature` ~60% figure were measuring **value non-emptiness, not key presence** — the keys are always in the schema. Exactly ONE genuinely optional key schema-wide: **`text.citations_grouping_mode` (~0.1%)** — the single allowlist carve-out the v1.1 field-level detector needs. v1.1 detection operates at key-presence level; value-semantics is a separate concern.
- **Export is a multi-file BUNDLE, not a loose file** [S14]. Observed shape: `conversations.json` + `memories.json` + `users.json` + `projects/` folder. **Floor input is `conversations.json` ONLY.** `memories.json` = model-generated/derived (reworded → barred from floor by the verbatim invariant; inventory-only). `users.json` = reference. `projects/` = metadata-only this export (no hidden conversation data; re-check each export). Targeting rule for the pipeline: resolve `conversations.json` *within a given export dir*, never trust the bare default source path (a fresh export at the default path = silent second full baseline, since new mtime+sha → new snapshot_id the idempotency guard won't refuse). To be enshrined as spec **§2.0** in the S15 spec pass.
- **`raw.json` wipe-vs-retain: RESOLVED S15 — Path A ratified** (see the RESOLVED block above the Settled Invariants). The invariant text reads *"raw wiped, sanitized snapshot = floor"*; the as-built code sealed `raw.json` `0o444` but never wiped it. Path A wipes it after Stage 3 verify-PASS (kills cred-at-rest, makes code match canon) + never stages it to any retained/backed-up surface (incl. NAS). Canon-side ratification DONE; the v1.3 unlink (NEXT MOVE #4) is CC/Jake's hands.
- **Skills-catalog crawl + SCDD sister track: SCDD at S3 (S14 cross-track).** Full GitHub catalog crawl (51,302 repos / 8,287 READMEs); chunker now multi-label v0.3. SCDD S2 closed the Continuous-Claude-v3 read (**function FAIL** — compresses corpus via headless-Claude into BGE embeddings → auto-DQ on the pointer-vs-compression invariant; does NOT gate us). Substrate face-off = **6 viable + NornicDB as a NEW lead candidate** (graph+vector+temporal, `orneryd/NornicDB`), pending its dual-gate. Face-off v2 lock is gated on NornicDB's dual-gate (SCDD D13); v2 is imminent. **Anti-FOMO confirmed at population scale** — SCDD's clean-room re-judgment re-derived every existing candidate + DQ, zero new ones; the candidate field is stable. SCDD's §3.2 shape gate fired on 4 live tools (claude-tap ~900★, etc.) that match the REFUSED capture shape — the wall generalizes exactly as S10 predicted.
- **Reference repo:** brought current by this **v8 enshrine** + S14→S15 handoff. Pipeline script unchanged this session (`402100d`, v1.2). Spec unchanged (v3) — the §2.0 + delta + scrub-vN + v1.1 spec pass is HELD for S15. All in `claude-reference` repo (no fork).

## NEXT MOVE (ordered)
*D9 LOCKED at S16 (substrate = Supabase, append-only enforced, ndjson canonical). The substrate question — the last one-way door — is CLOSED. The live targets are now the v1.3 build, then the seed-shape LOAD, then the retrieval layer.*
1. **Substrate lock — DONE (S16, D9 RESOLVED → Supabase).** See the D9 RESOLVED block + the locked-substrate Settled Invariant + `harness/D9_LOCK_2026-05-29.md`. NornicDB is the proven upgrade path (rebuild from ndjson, no integrity re-open). Nothing further here — kept as #1 only as the closed anchor that everything below now assumes.
2. **Build delta + scrub-vN + v1.1 (the v1.3 build) — the live build target.** From `Freeze_Pipeline_Spec_v4` (DONE on disk). Store-independent; the 5-28 export is the delta fixture (1,337 net-new msgs). Delta = uuid-set-difference (date never a filter; resolve `conversations.json` within the export dir, never the bare default path — the silent-second-baseline trap). Scrub-vN overlays. v1.1 field-level key-presence drift detection (allowlist per object type; one carve-out `text.citations_grouping_mode`). Roll in the **raw.json wipe** (one-line unlink after Stage 3 verify-PASS — canon RESOLVED S15) per "roll all pending into current build." Plan-OC/build-CC, plan-mode, **ratify a small batch before scale** (append-only ⇒ un-ratified rows are immortal). v1.2 → v1.3.
3. **Seed-shape LOAD (production ingest into Supabase).** Distinct from the S16 gate (which proved the store accepts + protects the shape): this is the real load of the floor-of-record into the locked Supabase. Use a **single-transaction / staging-swap** ingest (post-lock rec #6) so a partial failure never needs an append-only-violating DELETE. The `display_content` strip is a load-time toggle decided here. After load, run the deferred ⑤ branch-aware vector check IF/when the retrieval layer adds vectors (target = the 9 forests).
4. **Retrieval layer — the real next chapter.** The interface a live Claude session queries to get continuity from the floor. Research-shaped (embedding choice, branch-aware recall per FaceOff §10.5, the felt-rightness-of-recall problem). The three escalations (recall / kept / shared-memory) feed THIS layer, still blocked on Jake's uploads. Reversible (a weak retrieval approach gets swapped; the locked floor doesn't), unlike the substrate.
5. **`raw.json` wipe — code (v1.2 → v1.3).** Canon RESOLVED S15 (Path A). Remaining = the one-line unlink; CC/Jake's hands; rolled into the #2 build batch.
6. **Post-lock hardening (queued, not blocking — from the D9 RESOLVED block):** off-site immutable ndjson copy w/ object-lock (the ndjson is now the SPOF — highest priority), PITR or documented ndjson-rebuild RTO, pre-ingest lint, secondary index for tree traversal, CHECK constraints, **rotate the leaked DB password.**
7. **Archive backfill / export-cadence helper / per-project anchor passes** — later; helper depends on delta existing first.
8. **Storage-seam endgame** — Supabase/Postgres MCP connector on Jake's account; verify with a live round-trip. SCDD flagged seam gems: `alexander-zuev/supabase-mcp-server` (default stack — now the locked stack).
- **CHEAP WINS (S9-vetted, still apply):** **berserkdisruptors/contextual-commits** (adopted at S10); slot in **Ruya-AI/cozempic** (or **0xhimanshu/governor**) for long-session hygiene; evaluate **PrefectHQ/colin** for anchor currency.

### records.ndjson shape (locked at S13 — the seed shape substrates must support)
*Per-message record:* `{snapshot_id, scrub_version, conv_uuid, msg_uuid, parent_message_uuid, sender, created_at, updated_at, text, content_blocks[], attachments[], files[], is_root}`. *Per-conversation header:* `{record_type:"conversation_header", snapshot_id, scrub_version, conv_uuid, created_at, updated_at, account_uuid, message_count, has_branches, multi_root}`. NO `name`, NO `summary` anywhere. Records interleaved: header → that conv's messages (created_at order) → next header. Pointer = `(snapshot-id, conv_uuid, msg_uuid)`.

## GRAVEYARD

### ⛔ REFUSED — DO NOT RE-OPEN ⛔
> *The mechanism below was specified, partly proven, then **refused on principle** post-S10. The receipts are kept here as history so a future instance doesn't re-derive the same dead end and call it new. **THIS IS CLOSED.** If a fresh read makes the capture mechanism look attractive, **that is the failure mode this wall exists to prevent.** Sanctioned input only — see CURRENT STATE and the floor-input invariant.*
>
> *The specific refusal: standing up a pipeline that hooks claude.ai's internal `chat_conversations` history-fetch endpoint via a browser extension, and persisting the response payloads (including thinking blocks) to a local store. The mechanism is technically tractable; the answer is still no. The reasons hold regardless of intent or whose data it is: it reverse-engineers an undocumented internal surface, it systematically persists hidden reasoning Anthropic deliberately doesn't expose through sanctioned channels, and "it's your own account" doesn't transfer the property to the tool — the tool's shape is what generalizes. Don't re-litigate. Redirect to the official export.*

- **`chat_conversations` history-fetch hook as a capture surface** — REFUSED (post-S10).
- **Browser-extension live-capture / "kill the export ping by hooking the network"** — REFUSED (post-S10). Friction on the sanctioned path is real but is not a load-bearing reason; the export cadence helper (NEXT MOVE #6) addresses it without crossing the line.
- *(The S10 receipts below are kept as record of what was learned on the rejected path. They are NOT signposts to revisit it.)*
- "Thinking is live-SSE-only — capture it live or lose it" — falsified by the S10 probe; S11 then confirmed thinking IS in the export.
- "DOM scrape would be a viable floor" — lossy; would drop thinking and the message tree.
- "The scrub only needs to redact message text" — wrong; must walk all content-block types because `tool_result` is the prime cred vector. **This lesson is live** and reinforced by S12 (creds found in `display_content.json_block`, `attachments[].extracted_content`, and even thinking blocks).

### Standing graveyard (carried, still dead)
- **Corpus partitioned by `track` / a `track` column** — KILLED (S2). Re-walls the one-body corpus; hides cross-domain knowledge.
- **"The export's project field IS the track value"** — KILLED (S2; re-confirmed S11; re-re-confirmed S12 Check 7 — no `project` key at any depth).
- **The verbatim-COPY corpus / `corpus_seed_v1` / the char-diff fidelity gate** — KILLED (S9).
- **Manual curation of "salient" exchanges** — KILLED (S9). Replaced by no-discard + retrieval.
- **"Quarantine the cred forever"** — KILLED (S9). Replaced by scrub-at-freeze + wipe-the-raw.
- **Claude-AUTHORED per-chat verbatim floor** — DEAD (S9).
- **Parallel chat sessions for gathering** — DEAD (S9). The 12-instance bus trap; gathering = one scaled CC agent.
- **"More files / bigger window fixes it"** — DEAD. Storage was never the lever.
- **"Footer-date freshness tripwire is sufficient"** — DEAD. Checks the wrapper, not the payload.
- **Date as delta-ingest filter (or as performance hint alongside uuid-diff)** — KILLED (S11). Uuid only.
- **User-affected conv-level metadata treated as floor (or as "mutable hint")** — KILLED (S11, with pre-commit inversion correction). Both `name` and `summary` are user-affected; neither is floor.
- **In-place re-scrub when regex set extends** — KILLED (S11). `scrub-vN/` overlay model.
- **"`signature` is null in export (stripped)"** — KILLED (S12). Populated on ~60% of thinking blocks (8,087/13,393); base64 cryptographic provenance metadata. The v5 claim was a five-sample coincidence; the field IS in the export and treated as opaque blob by the scrub.
- **"`chat_messages` is strictly a tree, exactly one root per conv"** — KILLED (S12). 9/294 convs are forests with 2–6 sentinel roots. Pipeline handles multi-root via `is_root` + `multi_root` boolean.
- **"S10 cred counts (97 RTSP / 55 Postgres / 6 OpenAI) are the Stage 3 verify-gate baseline"** — KILLED (S12). S10 scan was narrower than full recursive descent. Population-validated baseline: RTSP=177, Postgres=76, OpenAI=10, Anthropic=10, Stripe=0.
- **"Five-sample probes characterize field semantics"** — KILLED (S12). Five samples test skeleton shape only; field-presence and field-semantics claims that land in canon need whole-archive scans. The `signature` drift error was exactly this failure mode.
- **"Stage 4 per-message record carries `conv_name`"** — KILLED (S12 — propagation miss from S11 inversion correction caught at S12 turn 8). Both `name` and `summary` dropped at ingest, symmetric application of the floor rule; no carry-through to per-message record.
- **"`display_content` is always a redundant render-mirror of `content[].text`"** — KILLED (S14). 15.6% of tool blocks (3,906) are CARRIES_UNIQUE — `display_content.code` holds file text recoverable nowhere else in the record (for `local_resource` items it's the only inline copy). Blanket strip ruled out; the top-5-records "always a mirror" read was the same projection/sample trap. Strip is selective + load-time only.
- **"The 5-28 export has field-level schema drift (`account_uuid`→`account` rename, new top-level `text`, etc.)"** — KILLED (S14). Projection-vs-raw artifact: the comparison used the projected `records.ndjson` field names against the raw new export. Raw-vs-raw confirms ZERO drift. The S13 code already reads `conv['account']['uuid']` + `msg['text']` from the baseline raw — those fields were never new/renamed.
- **"§4.5 population percentages (`name` 93.9% / `summary` 49.7%) measure key-presence"** — CLARIFIED/KILLED (S14). They measure VALUE non-emptiness. Keys are 100% present (conv 7 / msg 9). Don't build "key may be absent" logic off those figures.
- **"Anthropic could add new candidates we missed (FOMO on the substrate field)"** — KILLED at population scale (SCDD S3). Clean-room re-judgment re-derived every candidate + DQ, zero net-new. The field is stable; stop re-broadening it.
- **"NornicDB's code-default-off means decay is safe out of the box"** — SHARPENED, not killed (SCDD S5). The *code* default is off (`decayEnabled` zero-value false; decay is read-path ranking score, not storage eviction; no GC worker on engine-open) — BUT the shipped `nornicdb.example.yaml` sets `decay_enabled: true` + `auto_links_enabled: true` + `embedding.enabled: true`. A copy-paste deploy from the vendor example silently enables a ~2-second decay worker + an embedder that both touch frozen nodes. **Therefore D20's pin-everything-off is a HARD lock-time gate, not optional hygiene** — it walls off a landmine the vendor's own example walks you into. (Corrects an S4-read framing that called decay-OFF belt-and-suspenders: it's load-bearing if NornicDB is the pick.)

## CONFIDENCE FLAGS
- **Thinking-in-export** — CONFIRMED (S11). Full schema verified.
- **`signature` semantics** — CONFIRMED as base64 cryptographic provenance (S12). Not stripped, not content; opaque blob treated as string by scrub.
- **Tree vs forest** — CONFIRMED tree-or-forest, 9/294 multi-root (S12). Pipeline handles via `is_root` + `multi_root`.
- **UUID global uniqueness within archive** — CONFIRMED (S12 Checks 1+2). 22,801/22,801 msg, 294/294 conv.
- **Cross-export uuid stability** — CONFIRMED STABLE (S14). S14 export characterization (5-28 export vs baseline 5-25): all 22,801 baseline (conv_uuid, msg_uuid) tuples survived; 0 disappeared. 1,337 net-new tuples = clean delta payload. Tuple-level pointer is a durable key across re-exports.
- **Field-level schema drift across exports** — VERIFIED ZERO at population (S14, raw-vs-raw, all object types). 5-28 is structurally identical to baseline. BUT the v1.0 detector is type-level only — a future field add would pass silently; v1.1 key-presence detection is MANDATORY before relying on the detector across exports.
- **`display_content` semantics** — CHARACTERIZED at population (S14). 84.4% strip-safe / 15.6% CARRIES_UNIQUE (floor-grade, esp. `local_resource`). Strip is selective + load-time; floor keeps everything.
- **Key-presence vs value-presence** — SEPARATED (S14). Conv 7 keys / msg 9 keys = 100% key-present, no optional fields at key level. One optional key schema-wide: `text.citations_grouping_mode` (~0.1%). §4.5 %s are value-presence.
- **Record-size ceiling** — CONFIRMED (S14). Max 3.0 MB (`tool_result.content[].text`, not attachments — corrects S12). 19 records >1 MB, 0 >5 MB. Gate substrates against 3 MB.
- **Retention/retrieval substrate** — **LOCKED → Supabase (S16, D9 CLOSED).** Was FaceOff-v2-delivered/decision-open at v9; the seed-shape gate ran against the real `records.ndjson` on a dedicated Supabase project and PASSED 4/4, OC blind-re-read + `/jedi-council` (LOCK-WITH-CAVEATS 6/10, 0 P0, 1 P1, 5 caveats) → all 6 remediated with proof → Jake ratified. NornicDB is the proven upgrade path (rebuild from ndjson). See the D9 RESOLVED block.
- **Floor append-only enforcement** — **CONFIRMED ENFORCED (S16).** `floor_immutable_guard()` triggers (TRUNCATE/DELETE/UPDATE, no escape hatch) + REVOKE from app roles; proven by a live TRUNCATE rejected at BOTH the privilege layer (service_role) and the trigger layer (postgres owner), DELETE+UPDATE rejected by trigger, INSERT-still-works on a throwaway, floor intact at 23,095. This was the jedi-council's sole P1 — the "immortal floor" was a social convention with zero DB enforcement until remediated. Don't regress this: append-only must stay enforced, not assumed.
- **ndjson-canonical / Postgres-rebuildable** — **CONFIRMED (S16).** Rebuild-from-ndjson reproduced both tables with 0 value-mismatch across 6 directions. The ndjson is the canonical immortal artifact; Postgres is a derived index. This is what makes the lock low-regret (a wrong store is recoverable) AND makes the ndjson the SPOF (off-site immutable copy is the top post-lock item). ndjson SHA-256 `4ef22940…`, 367,494,497 B.
- **Floor fidelity claim** — **CORRECTED (S16).** NOT "verbatim" unqualified. Precise: value-preserving for JSONB (Postgres reorders object keys alphabetically + strips whitespace; ZERO value loss — measured 200/200 content_blocks reordered, 0 value-loss), byte-verbatim for TEXT columns + timestamps (`text` not `timestamptz` — deliberate), modulo scrub-v1 secret redaction (273 creds → tokens; correct archive behavior, but means not byte-identical to the raw export). Round-trip comparison is value-identity (arrays order-sensitive, objects order-insensitive), the correct test.
- **NornicDB byte-identical round-trip** — **CONFIRMED EMPIRICALLY (SCDD S5, closes D21).** Was PASS-on-mechanism / empirical-pending (the lone soft axis: two independent dual-gate reads rated it PASS-on-mechanism, but the only existing large-node test exercised *embeddings*, not a multi-MB *text* body — so byte-fidelity on the 3 MB string-property path was unproven). Closed 2026-05-28: a **4,122,695-byte inline string property** (≈1.37× the 3.0 MB max record), adversarially packed (quotes/braces/backslashes/emoji/CJK/newlines), written→read **byte-identical, sha256-verified, AND stable across a full container reboot** (cold BadgerDB reopen → same sha256). NornicDB v1.1.2, maximally-inert config (decay/auto-links/embeddings/bm25/vector/mcp/heimdall all OFF). On-disk vlog (4,123,037 bytes) survived the reboot — physically confirms vlog routing. Acceptance contract now **4/4 with no hedge.** Binding per-node ceiling: **~16 MiB** (the `walMaxEntrySize` anti-corruption guard on the WAL append/recovery path; one node write = one WAL entry) — 5.3× over our 3.0 MB max; `maxNodeSize` 50KB is an embedding-split threshold, not a reject; msgpack decode ceiling 256 MiB beyond that. ("No hard cap" from an early read is corrected: 16 MiB WAL is the real binding number.) Evidence: FaceOff_v2 §6.4.
- **Cred-baseline at population scale** — CONFIRMED (S12 Check 5). Locked at RTSP=177, Postgres=76, OpenAI=10, Anthropic=10, Stripe=0.
- **Stage 0** — CLOSED (S13). §4.5 ratified the symmetric-drop at population scale; no canon overturned.
- **Stages 1–4** — IMPLEMENTED + acceptance-passed (S13). `apparatus_freeze_pipeline.py` v1.2, baseline snapshot `baseline-2026-05-25-ae015455`, 9/9 acceptance checks. The architecture absorbed implementation with zero spec deviations — the strongest possible confirmation of the frozen design.
- **Pipeline coverage** — PARTIAL. Baseline + scrub-v1 done; delta runs + scrub-vN overlays + v1.1 field-drift detection spec'd (v4) but UNIMPLEMENTED — the S15 build target (#3). 5-28 export is the ready delta fixture (1,337 msgs).
- **Our memory MODEL is differentiated** — HIGH. No public repo stores verbatim-by-pointer-into-an-immutable-snapshot; they all extract/summarize/compress. Mechanics borrowable; model is ours. (Reinforced S14: SCDD's §12 convergence note — three function-fail candidates independently split raw-immutable / derived-queryable, same as our `raw`/`scrub-vN`; the difference is they compress the derived layer, we keep verbatim pointers.)
- **Within-session drift mitigation** — held clean S2→S14. S14 specifically: caught CC's unprompted anchor writes (§7.6), CC's own UUID false-positive (self-audited), and a projection-vs-raw field-drift artifact (OC SSE catch → CC raw-vs-raw redo → zero drift). The peer-review/gate loop keeps catching confident-canon errors before they land. Keep watching — the layer-confusion class (projected output vs raw input) recurred twice this session; it's a standing trap.

---
*Anchored v10 5-29-26 (apparatus S16 — **D9 SUBSTRATE LOCKED → Supabase**; floor append-only ENFORCED; ndjson canonical / rebuild PROVEN). v9→v10: executed the seed-shape gate against the real baseline `records.ndjson` (23,095 records, on box at `…/baseline-2026-05-25-ae015455/scrub-v1/`) on a dedicated Supabase project (`apparatus-floor`, SB Pro, separate from Cypher — the immutable-floor/plastic-layer blast wall). (1) **Harness PASSED 4/4** — ① ingest 23,095, 0 errors/rejects; ② store-enforced pointer uniqueness (22,801 distinct + UniqueViolation fired); ③ value-identity round-trip incl. the 2.93 MB max record (arrays order-sensitive, objects order-insensitive — the correct test for JSONB); ④ 9/9 forests reconstruct, sentinel-parent 304/304, zero orphans. (2) **Two errors caught + corrected, both assert-don't-verify class:** the seed-shape spec's "roots have null parent" was FALSE (roots carry sentinel `00000000-0000-4000-8000-000000000000`; zero nulls across all 22,801) — CC caught it against the disk, spec corrected v1→v1.1; and (the bigger one) the floor-is-immortal *claim* was UNENFORCED — the jedi-council's live grant-check found anon/authenticated/service_role all held TRUNCATE with zero guard triggers. (3) **Lock discipline:** OC blind-re-read the manifest cold (evidence reconciled independently), THEN `/jedi-council` adversarial review with live DB access → **LOCK-WITH-CAVEATS 6/10, zero P0, one P1, 5 caveats.** (4) **All 6 caveats remediated WITH PROOF before the lock:** append-only ENFORCED via `floor_immutable_guard()` (BEFORE TRUNCATE/DELETE/UPDATE, no escape hatch — strictly stronger than Supabase's `protect_delete`) + REVOKE from the 3 app roles, proven by a live TRUNCATE/DELETE/UPDATE REJECTED at both privilege + trigger layers, floor intact at 23,095; ndjson SHA-256 recorded (`4ef22940…`, 367,494,497 B) + companion artifact hashes; **rebuild-from-ndjson PROVEN 0-mismatch / 6 directions** (the reframe that dissolves the one-way door — Postgres is a rebuildable derived index, the ndjson is the canonical floor); "verbatim" relabeled precisely (value-preserving JSONB / byte-verbatim TEXT+timestamps / modulo scrub-v1); SB Pro tier confirmed (no auto-pause, headroom fine); account_uuid single-account confirmed. (5) **Jake ratified the lock 2026-05-29.** Enshrined: new **D9 RESOLVED block** + new **Settled Invariant** (floor substrate = Supabase, ndjson canonical, append-only enforced) + CURRENT STATE flipped to D9-locked + NEXT MOVE re-ordered (substrate DONE; the v1.3 build is now the live #2, then seed-shape LOAD #3, then the retrieval layer #4 — the real next chapter) + CONFIDENCE FLAGS (substrate LOCKED; enforcement CONFIRMED; ndjson-rebuildable CONFIRMED; fidelity-claim CORRECTED). Companion canon: Seed_Shape_Test_Spec v1→v1.1 (check-④ null→sentinel). Evidence bundle: `harness/D9_LOCK_2026-05-29.md` + `harness/s16_seed_shape_harness.py` + `harness/D9_remediate.py`. Process note: CC echoed the DB password to chat (set the env var inline in a logged command rather than reading the pre-set env) — rotate-at-leisure flagged in the D9 block + post-lock queue; lesson = CC reads creds from pre-set env, never inline. No prior Settled Invariant moved (the substrate was an OPEN cell being closed, not an invariant changed; the raw.json + floor-scoping invariants stand). Pipeline (v1.2, `402100d`) + spec (v4) unchanged on disk — the v1.3 build is still unbuilt and is the next executable move.*

---
*Anchored v9 5-28-26 (apparatus S15 — substrate FaceOff v2 delivered, D21 round-trip CLOSED, raw.json Path A ratified). v8→v9: folded SCDD S5 results + ratified the parked raw.json decision in-lane. (1) **Substrate FaceOff v2 DELIVERED** (SCDD S5; supersedes stale v1) — field settled at population scale (1,982 rows, zero field-expanding candidates); two co-leads both dual-gate PASS (NornicDB graph-native benchmark + Supabase+pgvector low-ops default); SCDD rec = Supabase-first-with-NornicDB-as-proven-upgrade-path, gated on the seed-shape test; **lock still apparatus's (D9), NOT YET LOCKED — recommendation delivered, decision open.** (2) **D21 round-trip CLOSED — empirical PASSED:** 4,122,695-byte adversarial inline string property written→read byte-identical, sha256-verified, stable across a full container reboot (cold BadgerDB reopen); NornicDB v1.1.2 maximally-inert; vlog survived reboot (confirms vlog routing). NornicDB acceptance contract 4/4 no hedge. The pre-vetted "3 MB → split-adapter" ding CLEARED: no hard per-node cap, binding ceiling is the ~16 MiB WAL-entry guard (5.3× over our 3.0 MB max), `maxNodeSize` 50KB is an embedding-split threshold not a reject. CONFIDENCE FLAGS flipped: retention/retrieval substrate OPEN→v2-delivered; NornicDB round-trip added as CONFIRMED. (3) **raw.json Path A RATIFIED** (Jake's call, made in the apparatus/main lane — SCDD deliberately left the invariant untouched and routed it back): raw is transient, wiped after Stage 3 verify-PASS (HARD-gated on PASS), and never staged to any retained/backed-up surface incl. the NAS; Anthropic's export is the byte-verbatim re-pull source. The invariant text already read "raw wiped"; Path A makes code match canon. Open "DECISION REQUIRED" block REMOVED; CURRENT STATE + NEXT MOVE flipped to RESOLVED. Code-side (v1.2→v1.3 unlink) is CC/Jake's hands, rolled into the #3 build batch. (4) **Graveyard sharpened:** the NornicDB decay-default — code is off, but `nornicdb.example.yaml` ships decay/auto-links/embeddings ON → D20 pin-everything-off is a HARD lock-time gate, not hygiene (corrects an S4-read framing). (5) **NEXT MOVE updated:** #1 substrate = v2-delivered/decision-open (not "imminent"); #2 v4 spec pass marked DONE (it already existed on disk — Jake landed it, the S14 handoff lagged; verified clean); #3 = the delta+scrub-vN+v1.1 BUILD (still unbuilt, the real S15 build target). No Settled Invariant moved (raw.json was a code/canon reconcile, not an invariant change — the "raw wiped" clause at §Secrets stands as written). Pipeline (v1.2, `402100d`) unchanged on disk; spec is v4 on disk (delta/scrub-vN/v1.1 spec'd, unbuilt). The open cells are now the substrate LOCK (D9, gated on seed-shape test) and the #3 build. Process note: an in-lane attribution cross-wire (which read found the 16 MiB ceiling / the open round-trip) was caught + corrected mid-session — facts held, stakes were the lesson; the blind-compare → reconcile loop worked.*

---
*Anchored v8 5-28-26 (apparatus S14 close, second-export + schema-stability verified). v7→v8: S14 was a read-only verification + reference session — zero pipeline code written, canon untouched until this enshrine. A second full export (5-28) was pulled and characterized against the 5-25 baseline, all at population scale. (1) Cross-export UUID stability CONFIRMED — UNVERIFIED flag cleared: 22,801/22,801 baseline (conv_uuid, msg_uuid) tuples survived in the new export, 0 disappeared; delta fixture sized at 1,337 net-new msgs (31 new convs + 141 appended). (2) Field-level schema drift VERIFIED ZERO (raw-vs-raw, every object type) — 5-28 is structurally identical to baseline; pipeline ingests it unchanged. (3) `display_content` characterized — NOT a universal mirror: 84.4% strip-safe, 15.6% CARRIES_UNIQUE (floor-grade file text, esp. `local_resource`); blanket strip ruled out, selective strip = deferred load-time transform. (4) Record-size landmines mapped — max 3.0 MB, driven by `tool_result.content[].text` (corrects S12's attachments suspicion). (5) Key-presence vs value-presence separated — conv 7/msg 9 keys 100% key-present; §4.5 %s are value-presence; one optional key schema-wide (`text.citations_grouping_mode`). Export confirmed a multi-file bundle (floor = conversations.json only). Graveyard added: display_content-always-mirror, 5-28-field-drift, §4.5-%s-are-key-presence, substrate-FOMO. Two process events: CC wrote canon unprompted (§7.6 violation — corrected, noodled, saved to CC memory); an early S14 "7 field drifts" finding was a projection-vs-raw artifact (OC SSE catch → CC raw-vs-raw redo → zero). NEXT MOVE re-ordered: substrate selection now #1 (gated on imminent SCDD face-off v2); the v4 spec pass (§2.0 bundle-targeting + delta + scrub-vN + v1.1 field-detection) is #2, HELD for S15. No invariant moved; pipeline (v1.2, `402100d`) and spec (v3) unchanged on disk. The open cells remain substrate selection (SCDD) and the v4 spec+build. v6→v7: S13 closed Stage 0 (§4.5 ratified the symmetric-drop — `name` 276/294 short/fragment, `summary` 146/294 long-prose, both always-string never-null, summary population non-monotonic confirming "may evolve," 0 summary-only convs; no canon overturned, §4.5 itself forced no spec change) and implemented Stages 1–4 as `apparatus_freeze_pipeline.py` v1.2 (commit `402100d`, tracked canon at `active/apparatus/`). The baseline snapshot `baseline-2026-05-25-ae015455` exists and acceptance-passed 9/9 (273 scrub redactions matching the locked cred-baseline, verify-clean PASS over 673,871 strings, 23,095 records, 9 multi_root, 3 empty, 304 root-position, raw.json read-only, 12 manifest fields, 1 ledger entry). The architecture absorbed implementation with ZERO spec deviations. **Spec reconciled v2→v3 at S13 close** — a fidelity pass folding three text-level drifts (§4.5 ANSWERED, §6 idempotency dry-run exemption, §6 schema-drift coverage corrected to match the v1.0 code); no invariant moved. Locked the records.ndjson seed shape. Added pipeline-coverage-PARTIAL flag (baseline + scrub-v1 only; delta + scrub-vN unimplemented). Re-ordered NEXT MOVE (Stages done; seed-shape ingest is now #1, substrate selection #2, delta/scrub-vN #3). No graveyard additions — S13 confirmed rather than overturned. Three in-session drift catches logged. Confidence HIGH that the frozen design is correct (implementation proved it; spec text reconciled to v3); substrate + cross-export uuid stability remain the open cells. v5→v6 (kept for record): S12 safety + robustness probes ran against the full 366MB archive. Falsified the v5 `signature` claim (populated on ~60% of thinking blocks, not stripped — base64 cryptographic provenance). Corrected the "`chat_messages` is a tree" claim to "tree-or-forest" (9/294 convs have 2–6 sentinel roots). Updated cred-baseline to population-confirmed counts (RTSP 97→177, Postgres 55→76, OpenAI 6→10, plus 10 net-new Anthropic `sk-ant-` hits including one inside a thinking block). Added new field clusters to inventory: `display_content` (~25K tool blocks), MCP metadata, `attachments[].extracted_content` (inlined file text up to 1.4MB, cred vector), `tool_result.content[]` five item types, the 9-path strip set. Added the sampling-floor invariant. Added the peer-review-protocol-visible working-style note. Dropped `conv_name` from Stage 4 per-message record (S11 propagation miss). Graveyard added: signature-is-stripped, tree-is-strict, S10-cred-baseline, five-sample-semantics-probes, conv_name-in-per-msg-record. v4→v5 (kept for record): folded S11 export verification + freeze-pipeline architecture + pre-commit inversion correction on `name` vs `summary`. v3→v4 (kept for record): folded the S10 capture mechanism's refusal + REFUSED-wall. v2→v3 (kept for record): folded S9 pointer-into-snapshot + S10 capture work onto v2 base.* S13 closed Stage 0 (§4.5 ratified the symmetric-drop — `name` 276/294 short/fragment, `summary` 146/294 long-prose, both always-string never-null, summary population non-monotonic confirming "may evolve," 0 summary-only convs; no canon overturned, §4.5 itself forced no spec change) and implemented Stages 1–4 as `apparatus_freeze_pipeline.py` v1.2 (commit `402100d`, tracked canon at `active/apparatus/`). The baseline snapshot `baseline-2026-05-25-ae015455` exists and acceptance-passed 9/9 (273 scrub redactions matching the locked cred-baseline, verify-clean PASS over 673,871 strings, 23,095 records, 9 multi_root, 3 empty, 304 root-position, raw.json read-only, 12 manifest fields, 1 ledger entry). The architecture absorbed implementation with ZERO spec deviations. **Spec reconciled v2→v3 at S13 close** — a fidelity pass folding three text-level drifts (§4.5 ANSWERED, §6 idempotency dry-run exemption, §6 schema-drift coverage corrected to match the v1.0 code); no invariant moved. Locked the records.ndjson seed shape. Added pipeline-coverage-PARTIAL flag (baseline + scrub-v1 only; delta + scrub-vN unimplemented). Re-ordered NEXT MOVE (Stages done; seed-shape ingest is now #1, substrate selection #2, delta/scrub-vN #3). No graveyard additions — S13 confirmed rather than overturned. Three in-session drift catches logged. Confidence HIGH that the frozen design is correct (implementation proved it; spec text reconciled to v3); substrate + cross-export uuid stability remain the open cells. v5→v6 (kept for record): S12 safety + robustness probes ran against the full 366MB archive. Falsified the v5 `signature` claim (populated on ~60% of thinking blocks, not stripped — base64 cryptographic provenance). Corrected the "`chat_messages` is a tree" claim to "tree-or-forest" (9/294 convs have 2–6 sentinel roots). Updated cred-baseline to population-confirmed counts (RTSP 97→177, Postgres 55→76, OpenAI 6→10, plus 10 net-new Anthropic `sk-ant-` hits including one inside a thinking block). Added new field clusters to inventory: `display_content` (~25K tool blocks), MCP metadata, `attachments[].extracted_content` (inlined file text up to 1.4MB, cred vector), `tool_result.content[]` five item types, the 9-path strip set. Added the sampling-floor invariant. Added the peer-review-protocol-visible working-style note. Dropped `conv_name` from Stage 4 per-message record (S11 propagation miss). Graveyard added: signature-is-stripped, tree-is-strict, S10-cred-baseline, five-sample-semantics-probes, conv_name-in-per-msg-record. v4→v5 (kept for record): folded S11 export verification + freeze-pipeline architecture + pre-commit inversion correction on `name` vs `summary`. v3→v4 (kept for record): folded the S10 capture mechanism's refusal + REFUSED-wall. v2→v3 (kept for record): folded S9 pointer-into-snapshot + S10 capture work onto v2 base.*
