# ANCHOR — apparatus-build track
*v5 · 2026-05-27 (apparatus S11 close, post-spec-freeze) · hot · read this + JAKE-RULES before working*

> **DIRECTIONAL NOTE (read first).** Between v3 and v4 the project's input mechanism changed: an extension-based capture of claude.ai's internal endpoints was specified, partly proven, then **refused on principle** and is permanently out of scope. **The input is now Anthropic's official conversation export, full stop.** See the **REFUSED — DO NOT RE-OPEN** wall in the GRAVEYARD for why.
>
> Between v4 and v5 the export was verified directly (thinking-in-export CONFIRMED; content-block enumeration closed) and the freeze-pipeline architecture was frozen as a real reference document (`Freeze_Pipeline_Spec_v1.md`). Most of the v4 architecture survives unchanged; new invariants land for floor scope, delta-ingest mechanism, and scrub versioning.

## DESTINATION
Build the apparatus's own memory system (*The Wallaby Way*): a closed, self-administered re-grounding loop that holds continuity for a stateless Claude across sessions — so Jake stops being the memory bus *and* the blind follower. It is **beta-Cypher-brain**: the same anchored/plastic strata Cypher will run on, hand-cranked now, hardening into Cypher's memory layer. Scope is **Jake's whole working surface**. A chat-Claude works small (anchor + rules), with a cold, complete, immutable **snapshot** behind it — built from official exports — that it reaches *by pointer* only when a *why* is disputed.

## WORKING STYLE (not invariants — how Jake works with the orchestrator)
*These are conventions Jake finds useful, not identity claims for the instance.*
- **Status line at the end of each reply** — Turn-end status stamp every reply — the format Jake uses is a backticked one-liner: turn N · ET-time · re-anchor X/4 · dest…; state…; next…. The turn counter and re-anchor cadence (every ~5 turns, with the 4/4 mark as a seam-hunt warning, not a guillotine) are how he holds thread-position across long sessions and knows when to wrap. It's a workflow, not a ritual — but he uses the specific format, so match it. **Timestamps: use bash `date`, never confabulate** — multi-hour gaps between turns happen.
- **Alignment check on request** — Jake may ask the orchestrator to play back its understanding of project state (goal, constraints, current state, next move). It's a check that we're pointed the same way, not a loyalty test. If the playback surfaces a disagreement, that's the point — say so.
- **Push back with evidence** — Jake relies on the orchestrator to catch his mistakes, not agree with him. Terse, peer-to-peer, no apology theater. If a request is wrong, off-base, or shouldn't be done, say so plainly.
- **Prose, not the widget** — answer in prose; don't reach for the ask_user_input tool to gather preferences.

## INVARIANTS — WITH WHY
*(consolidated across S1–S11; merged facets, no live why dropped — verify against the latest handoff if in doubt)*
- **One undelineated corpus; the anchors are the index/neurons INTO it** — BECAUSE partitioning by project hides cross-domain knowledge (a woodworking lesson is load-bearing for 3D finishing). S1: *"corpus without anchor is un-navigable noise; anchor = an index into it."* Don't re-wall the ocean.
- **Corpus = curated POINTERS into an immutable, named snapshot — never copied or reworded text. Claude authors the INDEX, never the FLOOR.** — BECAUSE copying is a drift vector inside the one artifact whose job is no-drift (the dead seed drifted 152×); the export is verbatim by construction; pointing deletes the vector. Claude curates whys well but *reconstructs* when asked to transcribe — the floor must be mechanical.
- **The floor input is the official Anthropic conversation export** — BECAUSE it's the sanctioned, supported, lossless source for Jake's own conversation data, carrying message uuids and the parent chain. No other input surface is in scope. (See the REFUSED wall for why.)
- **Floor = the message stream itself; user-affected conversation-level metadata is NOT floor.** [S11] — BECAUSE the scrub redacts cred substrings within content but never discards a message and never modifies a message uuid. Per-message content (text, blocks, attachments, files, timestamps, sender) IS floor — user messages and assistant messages are equal floor citizens. Conversation-level fields that Jake or the model mutate over time — `name` (user-renameable sidebar title) and `summary` (model-generated description, may evolve) — are NOT floor. Carrying either would treat session-affected metadata as evidence: a category error. Both dropped at ingest; conversation headers carry only stable structural data (uuids, timestamps, counts, branch-bool). Human-readable labels at retrieval are derived from the message stream itself or live in per-project anchors, never preserved as mutable strings in the snapshot layer.
- **Thinking blocks are floor-grade content alongside text** [S11: CONFIRMED present in export] — BECAUSE the architectural commitment is that any reasoning available in the snapshot is treated as evidence at the same fidelity as visible text. S11 verified directly: thinking blocks carry `{type, thinking, summaries, start_timestamp, stop_timestamp, cut_off, truncated, flags, alternative_display_type, signature}` with `signature` null in export and the rest populated.
- **Snapshot-id namespacing: freeze-and-name, accumulate, never overwrite** — BECAUSE exports are point-in-time and multiple will exist; a bare `conv/msg` pointer is ambiguous across them. Pointer = `{snapshot-id · conv_uuid · msg_uuid · why}`.
- **The pointer `(conv_uuid, msg_uuid)` is intrinsic to the message and stable across snapshots (presumed)** — BECAUSE it's a property of the message, not its array position; it survives a body scrub for free. Cross-export stability is still UNVERIFIED — confirm at next export by diffing a message present in two snapshots.
- **Ingest is delta-based via uuid-set-difference; date is never used as filter, hint, or proxy.** [S11] — BECAUSE date proxies break on backdated messages, dormant conversations reactivated after long gaps, and skipped exports (export Monday, miss Tuesday, run Wednesday — a date window misses Tuesday). UUIDs are intrinsic and survive all those cases. Two mechanisms doing the same job is a drift surface.
- **Scrub regex set is VERSIONED via `scrub-vN/` overlays; sealed snapshots never re-scrub in place.** [S11] — BECAUSE pattern sets evolve as new cred classes are observed (the S10 set was specific to what the archive contained — RTSP/postgres/openai). Re-scrubbing in place would mutate sealed snapshots; immortal storage with mutation is the failure mode we wall against. Overlays preserve append-only while allowing the pattern set to grow.
- **Navigable by retrieval, not curation** — BECAUSE no-discard + relevance-weighted retrieval is the genesis; hand-picking "salient" exchanges = Jake back on the bus + lost congeniality. The only curated thing is the tiny *why*, and it lives in the anchor.
- **Anchor hot + small + boot-read (instant, via git, no export needed); corpus cold + queried-never-booted (lazy, via export)** — BECAUSE the moment the corpus loads "just to be safe," the 87-file swamp is rebuilt. The hot/cold wall is the whole game; per-chat corpus freshness is a false requirement — the anchor carries current truth.
- **The presence layer can't be stored — only booted warm; the boot/re-read/working-cadence loop IS its mechanism; the lineage is how it survives not-persisting** — BECAUSE soup + neurons persist, but the mind that runs them resets every session. Continuity that doesn't depend on any single instance holding the thread.
- **Secrets never enter the corpus OR chat; cred scrubbed at freeze (logged + verified), raw wiped, sanitized snapshot = floor; verify-clean is a HARD gate** — BECAUSE a leaked cred in an immutable store is immortalized (one already leaked to chat). The scrub walks all content-block types via type-agnostic recursive descent over JSON strings (`tool_result` is the prime vector — command output, file dumps). Verify-fail halts ingest and quarantines the scrubbed file; ingest can never proceed with a hit. Value ≠ decision-content, so fidelity survives redaction.
- **Corpus is evidence; the anchor is the verdict; the graveyard is what's overturned** — BECAUSE immutable storage holds dead decisions at full conviction forever; only the anchor (+ graveyard) knows what's currently true.
- **All writes proposed + ratified by Jake in a batch; anchor curation is a SHARED two-navigator job** — BECAUSE Claude is the drift-prone summarizer and Jake's confirm is the second navigator that closes the dead error-correction seam. Claude proactively nominates anchor candidates at decision-time, filtered by *"does this bind future work"* (the `.env` proof: Jake couldn't see its centrality in the moment; Claude could).
- **Invariants carry a destination-level WHY, not a mechanism** — BECAUSE Jake isn't a coder; the why is what lets him drift-catch Claude without holding the how. The keystone that makes the loop self-correcting, not just self-transferring.
- **Apparatus tables stay SEPARATE from Cypher's live schema** — BECAUSE coupling daily build-context to a mid-migration schema means a 1c migration could brick the thing we build with.

## CURRENT STATE
**S11 closed with the freeze-pipeline architecture frozen as a real reference document and two UNVERIFIED cells closed.** The spec (`Freeze_Pipeline_Spec_v1.md`) is the architecture; implementation is gated on four small Stage 0 probes.
- **Locator gate: GREEN** (S9 + S11 re-confirmed against the 366MB archive): 294 convs / 22,801 globally-unique msg uuids / explicit parent chain in `conversations.json`. Pointer scheme `(conv_uuid, msg_uuid)` is intrinsic to the message. Root sentinel: `00000000-0000-4000-8000-000000000000`.
- **Conversation object: 7 keys** (uuid, name, summary, created_at, updated_at, account-with-uuid-only, chat_messages). **Message object: 9 keys** (uuid, sender, text, content, created_at, updated_at, parent_message_uuid, attachments, files); all present on all 22,801 messages, no optional fields.
- **Thinking-in-export: CONFIRMED** [S11]. Full schema (`thinking`, `summaries`, `cut_off`, `truncated`, `start_timestamp`, `stop_timestamp`, `flags`, `alternative_display_type`, `signature`). `signature` is null in export (stripped); the rest are populated.
- **Content-block types enumerated: 5** [S11]. Across 67,275 blocks: `text` (28767), `thinking` (13393), `tool_use` (12612), `tool_result` (12489), `token_budget` (14).
- **`chat_messages` is a TREE, not a list** [S11]. Branched conversations preserved verbatim through pipeline; multiple messages can share a parent.
- **Cred-inventory: RUN** (S10). Real scrub targets in the archive = RTSP camera creds (97), Postgres/Supabase conn strings (55), OpenAI `sk-` (6). The "Stripe" hit was public `pk_live_`, no secret key present. Spec extends regex set proactively to include `sk-ant-` and `(sk|rk)_live_` patterns.
- **No project↔conversation linkage in `conversations.json`** [S2 + S11 re-confirmed]. No `project` field on conversation objects; no sibling files in the export bundle.
- **Conv-level `name` and `summary` are both user-affected, neither is floor** [S11; inversion corrected pre-commit]. `name` is the user-renameable sidebar title (Jake renames every session); `summary` is model-generated description (may evolve). Initial S11 call carried `name` as a "mutable index hint" and dropped `summary` — caught pre-commit as an inverted application of the floor rule (the actively user-mutated field is `name`). Corrected: BOTH dropped at ingest. Human-readable labels at retrieval are derived from the message stream or live in per-project anchors. Conversation headers carry only stable structural data.
- **Freeze pipeline architecture: FROZEN** [S11]. Per `Freeze_Pipeline_Spec_v1.md` — Stages 0–4 (Stage 0 = four probes blocking implementation; Stages 1–4 = freeze → scrub → verify-clean → ingest). Snapshot storage: `baseline-` (one-time full) + `delta-` (every subsequent export, uuid-filtered). Versioned scrub overlays via `scrub-vN/`.
- **Skills-catalog crawl: LANDED, ~27,000 entries.** Read `catalog_summary.md`, not the raw 27k; do the fit-judgment at one seat.
- **Reference repo:** brought current by this v5 enshrine + the S11→S12 handoff.

## NEXT MOVE (ordered)
1. **Stage 0 probes** (five small, per spec §4). Sanitized skeletons only; same protocol as S11. (0.1) Field shapes for `text`, `tool_use`, `tool_result`, `token_budget` — `tool_result` is the prime cred vector and highest-leverage probe. (0.2) Attachments + files: inlined content or reference-by-handle? Affects scrub scope. (0.3) Single `summaries` entry shape. (0.4) `token_budget` block shape. (0.5) Field-semantics characterization for `name` and `summary`: how often each is populated, observable signs of user-rename vs model-authorship. Closes the last hedge on the S11 floor-scoping decision.
2. **Implement Stages 1–4** per `Freeze_Pipeline_Spec_v1.md`. Single Python script. Test against the 366MB archive: should produce 1 baseline snapshot, scrub audit matching known counts (97 RTSP + 55 postgres + 6 OpenAI), verify PASS, 22,801 message records + 294 conversation headers.
3. **Substrate selection** — Supabase+embeddings vs memvid (single-file counter-design) vs claude-mem's retrieval rig as plumbing. `records.ndjson` is substrate-agnostic; choose informed by skills-catalog read + seed-shape feasibility.
4. **Seed-shape ingest + ratify** — prove the chosen substrate on a small real batch, ratify before scale (append-only ⇒ un-ratified rows are immortal).
5. **Archive backfill** — pipeline at scale over the existing 366MB export.
6. **Export cadence helper** — small local helper that watches Downloads for a new `conversations.json` and auto-runs the pipeline. Reduces friction to "click Export, walk away." Optional but high-value; lowers cost of letting exports go stale.
7. **Per-project anchor passes** — hot index/neuron anchors per walled track; `meta` + LRN from the archive.
8. **Cross-export uuid-stability check** — confirm a uuid survives a *re*-export (non-blocking; at next export). Design absorbs both outcomes via snapshot-id.
9. **Storage-seam endgame** — Supabase/Postgres MCP connector on Jake's account; verify with a live round-trip before trusting it.
- **CHEAP WINS (S9-vetted, still apply):** adopt **berserkdisruptors/contextual-commits** (adopted at S10 for anchor/graveyard capture); slot in **Ruya-AI/cozempic** (or **0xhimanshu/governor**) for long-session hygiene; evaluate **PrefectHQ/colin** for anchor currency.

## GRAVEYARD

### ⛔ REFUSED — DO NOT RE-OPEN ⛔
> *The mechanism below was specified, partly proven, then **refused on principle** post-S10. The receipts are kept here as history so a future instance doesn't re-derive the same dead end and call it new. **THIS IS CLOSED.** If a fresh read makes the capture mechanism look attractive, **that is the failure mode this wall exists to prevent.** Sanctioned input only — see CURRENT STATE and the floor-input invariant.*
>
> *The specific refusal: standing up a pipeline that hooks claude.ai's internal `chat_conversations` history-fetch endpoint via a browser extension, and persisting the response payloads (including thinking blocks) to a local store. The mechanism is technically tractable; the answer is still no. The reasons hold regardless of intent or whose data it is: it reverse-engineers an undocumented internal surface, it systematically persists hidden reasoning Anthropic deliberately doesn't expose through sanctioned channels, and "it's your own account" doesn't transfer the property to the tool — the tool's shape is what generalizes. Don't re-litigate. Redirect to the official export.*

- **`chat_conversations` history-fetch hook as a capture surface** — REFUSED (post-S10).
- **Browser-extension live-capture / "kill the export ping by hooking the network"** — REFUSED (post-S10). Friction on the sanctioned path is real but is not a load-bearing reason; the export cadence helper (NEXT MOVE #6) addresses it without crossing the line.
- *(The S10 receipts below are kept as record of what was learned on the rejected path. They are NOT signposts to revisit it.)*
- "Thinking is live-SSE-only — capture it live or lose it" — falsified by the S10 probe; S11 then confirmed thinking IS in the export, closing the question entirely under the sanctioned path.
- "DOM scrape would be a viable floor" — lossy; would drop thinking and the message tree. (Moot under the redirect; noted so the lesson isn't relearned.)
- "The scrub only needs to redact message text" — wrong; must walk all content-block types because `tool_result` is the prime cred vector. **This lesson is live** and carried into the export pipeline's scrub.

### Standing graveyard (carried, still dead)
- **Corpus partitioned by `track` / a `track` column** — KILLED (S2). Re-walls the one-body corpus; hides cross-domain knowledge.
- **"The export's project field IS the track value"** — KILLED (S2; re-confirmed S11). No project↔conversation linkage in `conversations.json`.
- **The verbatim-COPY corpus / `corpus_seed_v1` / the char-diff fidelity gate** — KILLED (S9). Copying verbatim text out of a verbatim archive then char-diffing the copy; replaced by pointers-into-snapshot.
- **Manual curation of "salient" exchanges** — KILLED (S9). Replaced by no-discard + retrieval.
- **"Quarantine the cred forever"** — KILLED (S9). Replaced by scrub-at-freeze + wipe-the-raw.
- **Claude-AUTHORED per-chat verbatim floor** — DEAD (S9). The floor is captured (now: from the export), never authored.
- **Parallel chat sessions for gathering** — DEAD (S9). The 12-instance bus trap; gathering = one scaled CC agent.
- **"More files / bigger window fixes it"** — DEAD. Storage was never the lever.
- **"Footer-date freshness tripwire is sufficient"** — DEAD. Checks the wrapper, not the payload.
- **Date as delta-ingest filter (or as performance hint alongside uuid-diff)** — KILLED (S11). Date proxies break on backdated/dormant/skipped-export cases; uuid-diff covers them all. Two mechanisms doing the same job = drift surface. Uuid only.
- **User-affected conv-level metadata treated as floor (or as "mutable hint")** — KILLED (S11, with pre-commit inversion correction). Both `name` (user-renameable) and `summary` (model-generated, may evolve) are user-affected and neither is floor. The interim "keep name as mutable index hint" call from S11 turn 17 was inverted-field-semantics (we thought `summary` was the user-renamed field; it's actually `name`) and got corrected pre-commit. The principle now lands clean: floor = message stream; conv-level mutable metadata = none.
- **In-place re-scrub when regex set extends** — KILLED (S11). Mutating sealed snapshots violates append-only; replaced by `scrub-vN/` overlay model.

## CONFIDENCE FLAGS
- **Thinking-in-export** — CONFIRMED (S11). Full schema verified directly.
- **Cross-export uuid stability** — UNVERIFIED. Check at next natural export by diffing a message present in two snapshots. Design absorbs both outcomes via snapshot-id.
- **Retrieval substrate** — OPEN, pending the skills-catalog read and seed-shape test.
- **Our memory MODEL is differentiated** — HIGH. S9 deep crawl headline: no public repo stores verbatim-by-pointer-into-an-immutable-snapshot; they all extract/summarize/compress. The mechanics around it are borrowable; the model is ours.
- **Within-session drift mitigation** — held clean across S2/S9/S10/S11 (including S10's redirect; S11's two self-corrections on conv-summary semantics and date-as-filter; and S11's pre-commit inversion correction on `name` vs `summary` floor scoping — caught by a peer-instance review of the close bundle before commit). Keep watching.

---
*Anchored v5 5-27-26 (apparatus S11 close, post-spec-freeze; with pre-commit inversion correction on `name` vs `summary` floor scoping). v4→v5: closed the thinking-in-export UNVERIFIED cell (CONFIRMED directly via S11 probes); added content-block-type enumeration (5 types) and the conversation/message field inventories to CURRENT STATE; added invariants for floor scoping (message stream is floor, user-affected conv-metadata is not), delta-ingest via uuid-only diff, and scrub versioning via `scrub-vN/` overlays; folded "date as delta filter / perf hint," "user-affected conv-metadata as floor," and "in-place re-scrub" into the graveyard; reordered NEXT MOVE to lead with Stage 0 probes (now five — added a field-semantics probe) and to put substrate selection before seed-shape (substrate-agnostic records.ndjson is the spec handoff, but seed-shape ratifies the chosen substrate not the abstract). **Pre-commit correction (S11 close, post-bundle):** initial S11 close had `name` carried in conversation headers as "mutable index hint" while `summary` was dropped — inverted field semantics (the actively user-mutated field is `name`, not `summary`). Caught by peer-instance review of the ignition prompt's ratify call; symmetric drop landed before this anchor reached the repo. v3→v4 (kept for record): folded the S10 capture mechanism's specification → refusal → redirect to sanctioned export; reframed working-style mechanics as conventions rather than invariants; added the **REFUSED — DO NOT RE-OPEN** wall. v2→v3 (kept for record): folded S9 pointer-into-snapshot + S10 capture work onto v2 base. Confidence HIGH on the spec and the corrected floor scope; cross-export uuid stability remains the open cell.*
